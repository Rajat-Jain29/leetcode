class Solution {
public:
    int arraySign(vector<int>& nums) {
        int f = 1;
        for(int i: nums){
            if(i > 0)
                f*=1;
            else if (i < 0)
                f*=(-1);
            else
                f*=0;
        }
        return f;
    }
};
