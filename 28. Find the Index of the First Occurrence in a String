class Solution {
public:
    int strStr(string haystack, string needle) {
        if(needle.size() == 0)
            return 0;
        int ind = haystack.find(needle);
        if( ind != haystack.size() )
            return ind;
        return -1;

        
    }
};
